# Core
pyyaml>=6.0
requests>=2.31.0
python-dotenv>=1.0.0

# Video Processing
ffmpeg-python>=0.2.0
opencv-python>=4.8.0  # 用于 CV 预扫描（可选，未安装时自动跳过相关功能）

# Image Processing
Pillow>=10.0.0
imagehash>=4.3.1

# ML / AI
openai>=1.0.0
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0

# Output Generation
lxml>=4.9.0

# Web UI
gradio>=4.0.0

# Document parsing (optional, for script upload)
python-docx>=1.1.0
pypdf>=4.0.0


# Optional: Whisper (choose one)
# openai-whisper  # For OpenAI Whisper
# faster-whisper  # For faster-whisper

# ------------------------------------------------------------------
# 本地模型依赖（v0.3 新增）
# 推荐在 Python 3.11 + CUDA PyTorch 虚拟环境中安装：
#   uv venv --python 3.11 .venv311
#   uv pip install --python .venv311/Scripts/python.exe -r requirements.txt
# ------------------------------------------------------------------
accelerate>=0.25.0
bitsandbytes>=0.43.0
insightface>=0.7.3
onnxruntime-gpu>=1.16.0
funasr>=1.0.0
modelscope>=1.13.0
soundfile>=0.12.0
librosa>=0.10.0

# 注意：CUDA PyTorch 需要单独按官网命令安装，例如：
#   uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
